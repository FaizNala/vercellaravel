{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "vercel-build": "composer install --no-dev --optimize-autoloader && php artisan config:clear && php artisan view:clear && php artisan route:clear"
    },
    "devDependencies": {
        "axios": "^1.6.4",
        "laravel-vite-plugin": "^1.0.0",
        "vite": "^5.0.0"
    }
}
